# GitHub 部署说明

## ✅ 已完成
- ✅ Git 仓库已初始化
- ✅ 所有文件已提交
- ✅ 主分支已设置为 `main`
- ✅ Git 用户信息已配置（Yuk11ivan / 320673961@qq.com）

## 📋 接下来需要您手动完成的步骤

### 步骤 1: 在 GitHub 上创建新仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `personal-portfolio`（或您喜欢的名称）
   - **Description**: `王明宇个人展示页`
   - **Visibility**: 选择 **Public**（公开）或 **Private**（私有）
   - ⚠️ **重要**：**不要**勾选以下选项：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
3. 点击 **"Create repository"** 按钮

### 步骤 2: 连接本地仓库到 GitHub

创建仓库后，GitHub 会显示一个页面，其中包含仓库的 URL。

1. 复制仓库的 HTTPS URL（格式类似：`https://github.com/Yuk11ivan/personal-portfolio.git`）

2. 在 PowerShell 中运行以下命令（将 `YOUR_REPO_URL` 替换为您复制的 URL）：

```powershell
git remote add origin YOUR_REPO_URL
```

例如：
```powershell
git remote add origin https://github.com/Yuk11ivan/personal-portfolio.git
```

### 步骤 3: 推送代码到 GitHub

运行以下命令：

```powershell
git push -u origin main
```

**重要提示**：
- 系统会提示输入用户名和密码
- **用户名**：输入您的 GitHub 用户名（Yuk11ivan）
- **密码**：如果您的账户启用了双因素认证（2FA），请使用 **Personal Access Token** 而不是密码

### 步骤 4: 获取 Personal Access Token（如果启用了 2FA）

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 设置：
   - **Note**: `Personal Portfolio Deployment`
   - **Expiration**: 选择有效期（建议 90 天或更长）
   - **权限**：勾选 **`repo`**（完整仓库访问权限）
4. 点击 **"Generate token"**
5. **立即复制生成的 token**（只显示一次！）
6. 在推送时，密码处输入这个 token

### 步骤 5: 启用 GitHub Pages（可选，用于在线访问网站）

1. 在 GitHub 仓库页面，点击 **"Settings"**（设置）
2. 在左侧菜单中找到 **"Pages"**
3. 在 **"Source"** 下选择 **"main"** 分支
4. 点击 **"Save"**
5. 几分钟后，您的网站将在以下地址可用：
   - `https://Yuk11ivan.github.io/personal-portfolio/`

## 🎉 完成！

推送成功后，您就可以在 GitHub 上看到您的代码了。

## 📝 后续更新

每次修改代码后，使用以下命令更新：

```powershell
git add .
git commit -m "更新说明"
git push
```

## ❓ 遇到问题？

- **认证失败**：确保使用 Personal Access Token 而不是密码
- **仓库已存在**：如果远程仓库已有内容，可能需要先拉取：`git pull origin main --allow-unrelated-histories`
- **权限错误**：确保您有该仓库的写入权限

